let navigationText = document.querySelector('h2#headerTwo');
let firstSection = document.querySelector('section:first-of-type');
let pointFive = Array.from(document.querySelectorAll('li')).find(item => item.textContent.trim() === 'Пункт 5');
let hatredLevelBlock = document.getElementsByClassName('hatredLevelBlock')[0];
